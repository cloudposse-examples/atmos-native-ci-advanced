name: Release
on:
  release:
    types: [published]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read
  checks: write

env:
  ATMOS_PROFILE: github

jobs:
  promote:
    runs-on: ["ubuntu-latest"]
    steps:
      - name: Install Atmos
        uses: cloudposse/github-action-setup-atmos@v3
        with:
          install-wrapper: false
          atmos-version: ${{ vars.ATMOS_VERSION }}

      - name: Checkout
        uses: actions/checkout@v6

      - name: AWS Auth
        shell: bash
        run: |
          atmos auth env --identity plat-dev/terraform --format dotenv | tr -d "'" >> $GITHUB_ENV
          atmos auth login --identity plat-dev/terraform

      - uses: cloudposse/github-action-docker-promote@v1
        id: promote
        with:
          organization: cloudposse-examples
          repository: atmos-native-ci
          registry: ${{ vars.ECR_REGISTRY }}
          from: sha-${{ github.sha }}
          to: ${{ github.event.release.tag_name }}
          use_metadata: false

    outputs:
      image: ${{ steps.promote.outputs.image }}
      tag: ${{ steps.promote.outputs.tag }}

  affected-staging:
    needs: [ promote ]
    runs-on: ["ubuntu-latest"]
    container:
      image: ghcr.io/cloudposse/atmos:${{ vars.ATMOS_VERSION }}
    defaults:
      run:
        shell: bash
    outputs:
      matrix: ${{ steps.affected.outputs.matrix }}
      has_affected: ${{ steps.affected.outputs.has_affected }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Describe Affected
        id: affected
        run: |
          MATRIX=$(atmos describe affected --format=matrix 2>/dev/null || echo '{"include":[]}')
          echo "matrix=$MATRIX" >> $GITHUB_OUTPUT
          if [ "$MATRIX" = '{"include":[]}' ] || [ -z "$MATRIX" ]; then
            echo "has_affected=false" >> $GITHUB_OUTPUT
          else
            echo "has_affected=true" >> $GITHUB_OUTPUT
          fi

  deploy-staging:
    needs: [ affected-staging, promote ]
    if: ${{ needs.affected-staging.outputs.has_affected == 'true' }}
    runs-on: ["ubuntu-latest"]
    container:
      image: ghcr.io/cloudposse/atmos:${{ vars.ATMOS_VERSION }}
    strategy:
      matrix: ${{ fromJson(needs.affected-staging.outputs.matrix) }}
      fail-fast: false
    name: deploy / staging / ${{ matrix.component }}
    environment:
      name: staging
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Deploy
        id: deploy
        env:
          APP_IMAGE: "${{ needs.promote.outputs.image }}:${{ needs.promote.outputs.tag }}"
        run: |
          atmos terraform deploy ${{ matrix.component }} -s ${{ matrix.stack }}

  affected-prod:
    needs: [ deploy-staging ]
    if: ${{ always() && needs.deploy-staging.result != 'failure' }}
    runs-on: ["ubuntu-latest"]
    container:
      image: ghcr.io/cloudposse/atmos:${{ vars.ATMOS_VERSION }}
    defaults:
      run:
        shell: bash
    outputs:
      matrix: ${{ steps.affected.outputs.matrix }}
      has_affected: ${{ steps.affected.outputs.has_affected }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Describe Affected
        id: affected
        run: |
          MATRIX=$(atmos describe affected --format=matrix 2>/dev/null || echo '{"include":[]}')
          echo "matrix=$MATRIX" >> $GITHUB_OUTPUT
          if [ "$MATRIX" = '{"include":[]}' ] || [ -z "$MATRIX" ]; then
            echo "has_affected=false" >> $GITHUB_OUTPUT
          else
            echo "has_affected=true" >> $GITHUB_OUTPUT
          fi

  deploy-production:
    needs: [ affected-prod, promote ]
    if: ${{ needs.affected-prod.outputs.has_affected == 'true' }}
    runs-on: ["ubuntu-latest"]
    container:
      image: ghcr.io/cloudposse/atmos:${{ vars.ATMOS_VERSION }}
    strategy:
      matrix: ${{ fromJson(needs.affected-prod.outputs.matrix) }}
      fail-fast: false
    name: deploy / production / ${{ matrix.component }}
    environment:
      name: prod
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Deploy
        id: deploy
        env:
          APP_IMAGE: "${{ needs.promote.outputs.image }}:${{ needs.promote.outputs.tag }}"
        run: |
          atmos terraform deploy ${{ matrix.component }} -s ${{ matrix.stack }}
